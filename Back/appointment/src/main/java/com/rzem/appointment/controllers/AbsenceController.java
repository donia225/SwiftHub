package com.rzem.appointment.controllers;

/*
import com.rzem.appointment.entities.Absence;
import com.rzem.appointment.entities.Availability;
import com.rzem.appointment.servicesImpl.AbsenceServiceImpl;
import com.rzem.appointment.servicesImpl.AvailabilityServiceImpl;
import lombok.RequiredArgsConstructor;
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

import java.util.List;

@RestController
@RequestMapping("/api/absance")
@RequiredArgsConstructor
@CrossOrigin(origins = "http://localhost:4200")
public class AbsenceController {


    private final AbsenceServiceImpl service;

    @PostMapping
    @ResponseStatus(HttpStatus.CREATED)
    public void save(@RequestBody Absence absence){
        service.save(absence);
    }

    @GetMapping
    public ResponseEntity<List<Absence>> findAll(){
        return ResponseEntity.ok(service.getAll());
    }


}


 */