eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

server:
  port: 8020

spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/workshopDB
